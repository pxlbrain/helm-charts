apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-alb-ingress.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "aws-alb-ingress.name" . }}
    helm.sh/chart: {{ include "aws-alb-ingress.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aws-alb-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
